[project]
name = "python-scrappy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.7",
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "pytest",
    "black", 
    "flake8",
    "django-debug-toolbar",
    "ruff",
]
